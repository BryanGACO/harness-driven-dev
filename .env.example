# Linear API key — get yours at: Linear → Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_your_key_here

# Linear team key — the prefix for issue IDs (e.g., DEMO-1)
# Only needed if your team key is not "DEMO"
# LINEAR_TEAM_KEY=DEMO
